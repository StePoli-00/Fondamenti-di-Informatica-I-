#include "matrice.h"

struct matrix *mat_rendiquadrata(const struct matrix *a)
{
	//struct matrix *out;
	double n = 0;
	//out = malloc((a->rows*a->cols)* sizeof(double));
	n= (a->cols) - (a->rows);
	/* if (n > 0)
		
		//out->data = calloc(n, sizeof(double));
	else
		//out->data = calloc(-n, sizeof(double));
	return a;
	*/
	}
	
	
